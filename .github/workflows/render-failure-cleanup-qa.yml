name: ProfitMente Studio Render Failure Cleanup QA

on:
  push:
    paths:
      - 'studio/studio_server.py'
      - 'studio/test_render_failure_cleanup.py'
      - '.github/workflows/render-failure-cleanup-qa.yml'
  workflow_dispatch:

jobs:
  render-failure-cleanup-qa:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Check server syntax
        run: python -m py_compile studio/studio_server.py studio/test_render_failure_cleanup.py
      - name: Verify failed render cleanup
        working-directory: studio
        run: python test_render_failure_cleanup.py
