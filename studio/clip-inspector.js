(()=>{
  const $=s=>document.querySelector(s);
  const props=$('.props'); if(!props)return;
  const panel=document.createElement('section'); panel.className='clipInspector'; panel.innerHTML=`<hr><h3>Clip seleccionado</h3><div id="clipInspectorEmpty" class="clipEmpty">Selecciona un clip en el timeline.</div><div id="clipInspectorForm" hidden>
  <label>Nombre<input id="ciName"></label><div class="ciGrid"><label>Inicio<input id="ciStart" type="number" min="0" step="0.05"></label><label>Duración<input id="ciDuration" type="number" min="0.25" step="0.05"></label></div>
  <label id="ciSpeedWrap">Velocidad<select id="ciSpeed"><option value="0.5">0.5×</option><option value="0.75">0.75×</option><option value="1">1×</option><option value="1.25">1.25×</option><option value="1.5">1.5×</option><option value="2">2×</option></select></label>
  <label id="ciVolumeWrap">Volumen<input id="ciVolume" type="range" min="0" max="2" step="0.01"><small id="ciVolumeValue"></small></label>
  <label id="ciSourceVolumeWrap">Audio original del video<input id="ciSourceVolume" type="range" min="0" max="2" step="0.01"><small id="ciSourceVolumeValue"></small></label>
  <label id="ciMuteWrap" class="ciCheck"><input id="ciMuted" type="checkbox"> Silenciar audio original</label>
  <label id="ciTransitionWrap">Transición<select id="ciTransition"><option value="cut">Corte</option><option value="fade">Fade</option><option value="slide">Slide</option><option value="zoom">Zoom</option></select></label>
  <label id="ciMotionWrap">Movimiento<select id="ciMotion"><option value="none">Ninguno</option><option value="slow-zoom">Slow zoom</option><option value="push-in">Push in</option></select></label>
  <label id="ciStyleWrap">Estilo caption<select id="ciStyle"><option value="dynamic">Dinámico</option><option value="hook-pop">Hook Pop</option></select></label>
  <label id="ciAnimationWrap">Animación caption<select id="ciAnimation"><option value="none">Ninguna</option><option value="pop">Pop</option><option value="word-pulse">Word pulse</option></select></label>
  <div class="ciActions"><button id="ciToCursor">Inicio = cursor</button><button id="ciFit">Ajustar al medio</button></div></div>`; props.appendChild(panel);
  const byId=id=>(project.clips||[]).find(c=>c.id===id); let renderedId=null;
  function selected(){return window.ProfitMenteEditTools?.selectedId||null}
  function val(id,v){const el=$('#'+id);if(el&&document.activeElement!==el)el.value=v??''}
  function render(){const c=byId(selected()),form=$('#clipInspectorForm'),empty=$('#clipInspectorEmpty');if(!c){form.hidden=true;empty.hidden=false;renderedId=null;return}form.hidden=false;empty.hidden=true;renderedId=c.id;val('ciName',c.name||'');val('ciStart',Number(c.start||0).toFixed(2));val('ciDuration',Number(c.duration||0).toFixed(2));const audio=[4,5,6].includes(c.track),visual=[0,1,2].includes(c.track),caption=c.track===3,a=c.asset&&assets.find(x=>x.id===c.asset),videoWithSource=visual&&a?.type==='video',speedable=!!a&&['video','audio'].includes(a.type);$('#ciSpeedWrap').hidden=!speedable;$('#ciVolumeWrap').hidden=!audio;$('#ciSourceVolumeWrap').hidden=!videoWithSource;$('#ciMuteWrap').hidden=!videoWithSource;$('#ciTransitionWrap').hidden=!visual;$('#ciMotionWrap').hidden=!visual;$('#ciStyleWrap').hidden=!caption;$('#ciAnimationWrap').hidden=!caption;if(speedable)val('ciSpeed',String(c.speed||1));if(audio){const v=c.volume??(c.track===5?.22:1);val('ciVolume',v);$('#ciVolumeValue').textContent=`${Math.round(v*100)}%`}if(videoWithSource){const sv=c.sourceVolume??1;val('ciSourceVolume',sv);$('#ciSourceVolumeValue').textContent=`${Math.round(sv*100)}%`;$('#ciMuted').checked=!!c.muted}if(visual){val('ciTransition',c.transition||'cut');val('ciMotion',c.motion||'none')}if(caption){val('ciStyle',c.style||'dynamic');val('ciAnimation',c.animation||'none')}}
  function commit(field,value,message){const c=byId(selected());if(!c)return;if(field==='start')value=Math.max(0,Math.min(project.duration-.25,+value||0));if(field==='duration')value=Math.max(.25,Math.min(project.duration-c.start,+value||.25));if(field==='volume'||field==='sourceVolume')value=Math.max(0,Math.min(2,+value||0));if(field==='speed')value=Math.max(.25,Math.min(4,+value||1));if(field==='muted')value=!!value;c[field]=value;if(typeof persist==='function')persist();if(typeof drawTimeline==='function')drawTimeline();if(typeof renderAt==='function')renderAt(+$('#playhead').value||0);if(typeof setStatus==='function')setStatus(message||'Clip actualizado');requestAnimationFrame(render)}
  const map={ciName:'name',ciStart:'start',ciDuration:'duration',ciSpeed:'speed',ciVolume:'volume',ciSourceVolume:'sourceVolume',ciTransition:'transition',ciMotion:'motion',ciStyle:'style',ciAnimation:'animation'};
  Object.entries(map).forEach(([id,field])=>{const el=$('#'+id);const event=['ciVolume','ciSourceVolume'].includes(id)?'input':'change';el.addEventListener(event,()=>{if(id==='ciVolume')$('#ciVolumeValue').textContent=`${Math.round(+el.value*100)}%`;if(id==='ciSourceVolume')$('#ciSourceVolumeValue').textContent=`${Math.round(+el.value*100)}%`;commit(field,el.value,`${field} actualizado`)})});
  $('#ciMuted').addEventListener('change',e=>commit('muted',e.target.checked,e.target.checked?'Audio original silenciado':'Audio original activado'));
  $('#ciToCursor').onclick=()=>commit('start',+$('#playhead').value||0,'Clip alineado al cursor');
  $('#ciFit').onclick=()=>{const c=byId(selected());if(!c||!c.asset)return;const a=assets.find(x=>x.id===c.asset);if(!a?.duration){setStatus?.('Este medio no tiene duración detectable');return}const speed=Math.max(.25,Math.min(4,Number(c.speed)||1));commit('duration',Math.min(a.duration/speed,project.duration-c.start),'Duración ajustada al medio')};
  document.addEventListener('click',e=>{if(e.target.closest?.('.clip'))requestAnimationFrame(render)},true);document.addEventListener('keydown',()=>requestAnimationFrame(render));
  const oldDraw=window.drawTimeline;if(typeof oldDraw==='function')window.drawTimeline=function(){oldDraw();requestAnimationFrame(render)};
  setInterval(()=>{if(selected()!==renderedId)render()},500);render();
})();